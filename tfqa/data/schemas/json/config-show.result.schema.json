{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://flashcrucible.local/schemas/config-show.result.schema.json",
  "title": "tfqa config show result",
  "description": "The merged configuration the CLI resolved. Validates CLIResponse.data for `tfqa config show`; the envelope itself is cli_response.schema.json.",
  "schema_version": 1,
  "type": "object",
  "properties": {
    "config": {
      "type": "object"
    }
  },
  "additionalProperties": true,
  "required": [
    "config"
  ]
}
